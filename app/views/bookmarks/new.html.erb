<div class="container col-lg-8">
  <h1>Add movie to <%= @list.name %>'s list </h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.button :submit, class: "btn btn-flat" %>
  <% end %>
</div>
